
# KPI Implementation Scope Note

The Python computation layer currently implements the original 7 KPIs from the student's prototype.
Additional KPIs included in `kg_mapping/` are currently documented as mapping concepts only.
This keeps the repository structure stable while allowing later extension of the executable pipeline.
